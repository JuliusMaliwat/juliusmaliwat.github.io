// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  // GitHub Pages user site (base URL is /).
  site: "https://juliusmaliwat.github.io",
  trailingSlash: "always",
});
